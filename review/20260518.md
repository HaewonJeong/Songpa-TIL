**오늘의 한 줄 평:** 

#### 꿀팁
- 
* 2026-05-18 수업 29일차
#### JDBC
- Java에서 Database에 접근 가능하도록 하는 Programming API 이다.
- 어떤 DBMS든 맞게 사용하게 해준다.
- 실습 환경
  - DBMS : MYSQL -> Driver
  - DB Server Address(DB 서버 주소) : Localhost -> URL
  - DB : menudb, employeedb -> URL
  - Account(계정) : ohgiraffers -> USER, PASSWORD
- NEW project > Build System : Gradle, Gradel SLD : Grove 선택
  - 기존 실습 시 빠르게 컴파일 할 때는 IntelliJ를 사용 했으나
  - Grove 선택 이유는 빌드 과정 자동화 도구 선택.
  - build.grade 파일 : 실행 파일 이 생성 됨.
- MYSQL Connector 
  - Java와 Database 연결을 위한 세팅 진행
  - 1. MVN REPOSITORY(자바 개발자들이 사용하는 오픈소스 라이브러리.) 접속
  - 2. MySQL Connector/J > 9.4.x (적당한 중간 버전) > Format: Groovy Short > 복사 > Build.gradle 파일 내 dependencies에 추가 > 코끼리 모양(새로고침) 클릭 > 적용
  - 우측 DataBase > 설정 > Apply 선택 > IDE에서 SQL 작성 가능한 환경 세팅 완료
- Main > Java > Applicaion1 파일에 Connect 만들기
- DriverManager.getConnection 구문 작성
```
Connection con = null;

        try {
            /*사용할 JDBC Driver 클래스를 로딩*/
            Class.forName("com.mysql.cj.jdbc.Driver");

            //DriverManager.getConnection(url, user, password) 어떤 DB & DBMS? 어떤 계정과 PW? 전달
            con = DriverManager.getConnection("jdbc:mysql://localhost/employeedb", "ohgiraffers", "ohgiraffers");

            System.out.println("con = " + con);

        } catch (ClassNotFoundException e) {
            e.printStackTrace();
        } catch (SQLException e) {
            e.printStackTrace();
        } finally {
            if(con != null){
                try {
                    if(!con.isClosed()){
                        con.close();
                    }
                } catch (SQLException e) {
                    e.printStackTrace();
                }
            }
        }
```