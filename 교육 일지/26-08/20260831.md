**오늘의 한 줄 평:** JJCinema (영화 예매 서비스) — Spring Boot + PostgreSQL
#### 꿀팁 
- 
# 2026-08-31 수업 102일차

---

오늘 한 일
1. 프론트 코드 리뷰 (AdminLayout.jsx)
관리자 권한 체크 여부, 탭 배열화, 반응형 대응 등 개선 포인트 점검

2. 백엔드 잔여 구현 범위 파악
Notion "API별 구현 로직 & 남은 목표" 페이지와 실제 JJCinemaBackend 코드를 대조
예상보다 진행이 많이 되어 있음을 확인 (좌석/예매/동시성 락/일부 결제까지 구현됨)

3. 모의 결제(PaymentController) 구현
Payment 엔티티, PaymentRepository, PaymentService, PaymentController 작성
POST /api/payments (forceResult로 성공/실패 강제 지정) + GET /api/payments/{bookingId} 구현
결제 성공 → Booking.confirm()(HELD→CONFIRMED), 실패 → 좌석 해제 후 Booking.cancel()

4. Postman으로 다중 좌석 예매 + 결제 흐름 테스트
Booking 1건이 좌석 여러 개를 대표하는 구조임을 확인 (좌석별 결제 아님, 예매 건당 결제 1건)
로그인 → 예매 생성(POST /api/bookings, seatCodes 배열) → 결제(POST /api/payments, bookingId 하나) 순서로 검증

5. 트러블슈팅
증상	원인
AdminService 컴파일 대량 에러	countSeats() 메서드에서 중괄호(}) 하나가 더 있어서 클래스가 조기 종료됨
findByBookingId 관련 에러	findByBookingIdIn을 BookingSeatRepository가 아니라 BookingRepository에 잘못 선언 (엔티티 타입 불일치)
로그인 실패가 500으로 떨어짐	AuthenticationException을 잡는 핸들러가 GlobalExceptionHandler에 없었음

6. 전체 코드 리뷰
AdminController가 빈 스텁이라 서비스 로직은 다 있는데 엔드포인트가 하나도 연결 안 된 상태 발견
User.updateByAdmin() 메서드 몸통이 비어있어 관리자의 회원 정보 수정이 조용히 무시되는 버그 발견
두 곳 다 코드 작성해서 해결

7. 로그인 응답 개선
로그인 응답을 문자열("로그인 성공") 대신 UserResponse(id/name/email/role)로 반환하도록 변경
비밀번호 오류/비활성 계정 로그인 시 500 대신 401로 처리하는 예외 핸들러 추가
오늘 배운 개념
Spring Data JPA 메서드명 규칙: find와 By 사이의 단어는 무시됨(findUserByEmail = findByEmail), 반대로 By가 아예 없으면 조건 없는 쿼리가 되어버림
**비관적 락(@Lock(PESSIMISTIC_WRITE))**으로 동시 예매(더블부킹) 문제를 막는 구조